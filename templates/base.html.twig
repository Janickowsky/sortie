<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}{% endblock %}</title>
        <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js" integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN" crossorigin="anonymous"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js" integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl" crossorigin="anonymous"></script>
        <link rel="stylesheet" href="https://bootswatch.com/4/minty/bootstrap.min.css">
        <link rel="icon" type="image/x-icon" href="{{ asset('build/images/favicon.png')}}">
        {{ encore_entry_script_tags('app') }}
    </head>
    <body>
        <header>
            <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
                <div class="ml-2 row d-flex align-items-center">
                    <a class="navbar-brand font-weight-bold" href="{{ url("home_home") }}">Sortir.com</a>
                    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                </div>
                <div class="d-flex justify-content-end collapse navbar-collapse">
                    <ul class="navbar-nav">
                        {% if app.user %}
                        <li class="nav-item mr-5">
                            <div class="dropdown">
                                <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown">
                                        {{  app.user.prenom }} {{ app.user.nom|slice(0, 1) }}
                                </button>
                                <div class="dropdown-menu">
                                        <a class="dropdown-item" href="{{ path('user_monProfil') }}">Mon Profil</a>
                                        <a class="dropdown-item" href="{{ url('app_logout') }}">Se deconnecter</a>
                                    {% if app.user and 'ROLE_ADMIN' in app.user.roles %}
                                        <a class="dropdown-item" href="{{ path('ville_listeVille') }}">Villes</a>
                                        <a class="dropdown-item" href="{{ path('site_listeSite') }}">Sites</a>
                                    {% endif %}
                                </div>
                            </div>
                        </li>
                        {% endif %}
                    </ul>
                </div>
            </nav>
        </header>
        <div class="container">
        {% block body %}{% endblock %}
        </div>
    </body>
    <footer class="bg-primary fixed-bottom">
        <div class="container d-flex justify-content-center">
            <p class="text-light font-weight-bold">&#169; | Janick Buisson, Benedicte Freulon, Mathieu Le Bouedec</p>
        </div>
    </footer>
</html>
